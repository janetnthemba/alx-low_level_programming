#include <unstd.h>
#include "main.h"

/*
 * _putchar - writes the character c to stdout
 * @c:The character to print
 *Return:On sucess 1 else -1
 */
int _putchar(char c)
{
	Return (write (1, &c, 1));
}
